<h2> <%= @recipe.title %></h2>

<h3>Ingredients</h3>

<ul>
<% @recipe.ingredients.each do |ingredient| %>
  <li> <strong> <%= "#{ingredient.name}" %></strong> | <em><%= "#{ingredient.quantity}" %></em></li>
<% end %>
</ul>
